import { PositionType } from "@icat/features/common";

export type SecondaryHeroProps = {
  badge?: string;
  title?: string;
  subtitle?: string;
  heroImageUrl?: string;
  position?: PositionType;
};
