/**
 * This barrel file is intentionally empty to encourage direct imports 
 * and avoid pulling in unnecessary server-side or database dependencies 
 * into client components and middleware.
 * 
 * Use direct imports instead:
 * import { Component } from "@icat/features/common/component-name";
 */
export {};
