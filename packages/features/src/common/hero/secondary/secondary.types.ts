import { PositionType } from "@icat/features/common/common.types";

export type SecondaryHeroProps = {
  badge?: string;
  title?: string;
  subtitle?: string;
  heroImageUrl?: string;
  position?: PositionType;
};
